#include "main.h"

/**
 * mul - multiply two integers
 * @a: first input parameter
 * @b: second input parameter
 * Return: the result of a * b
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
